# panel/frontend environment configuration.
#
# Copy this file to `.env` (git-ignored). Each subproject manages its own .env.

# Backend port the dev server proxies /api to. MUST match the PANEL_PORT set in
# panel/backend/.env, so the frontend proxies to the port the backend runs on.
PANEL_PORT=4321
