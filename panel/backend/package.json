{
  "name": "@textual/panel-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Textual panel — local-only backend: referential integrity + atomic file writes.",
  "license": "UNLICENSED",
  "scripts": {
    "test": "node --test \"lib/__tests__/*.test.ts\" \"routes/__tests__/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.6.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
